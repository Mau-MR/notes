# Installation of a debian file for ubuntu

```sh
sudo dpkg -i package_file.deb
```

```sh
sudo apt install ./<packagename>.deb
```
## uninstall debian .deb package

```sh
sudo apt-get remove package_name
```